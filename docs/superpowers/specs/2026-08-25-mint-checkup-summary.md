# DIMENSION 铸造准备卷 T7 — 800-hash 大规模废片体检 统计摘要

> 收卷件（2026-08-25）。DIMENSION HEAD=`36ee79f`（体检只读，未改仓），测试基线实跑复核 422/422。
> 完整报告（含方法学与全部分组表）在 sdf-main `.superpowers/sdd/2026-08-25-dimension-mint-readiness/task-7-report.md`
> （gitignored 工作档）；最差 20 件图集 + overview sheet 落 `~/Downloads/genlab-mint-checkup-batch13/`。

## 方法一句话

800 枚 `crypto.randomBytes` 随机 hash，链语义自然路由（`dev.html?hash=` → `window.tokenData`
注入契约，零 dev 覆盖 → 维度轮盘自然落点），真浏览器 headless Chrome + CDP 渲染，完成信号
`$renderOK`（与铸造截图同触发点），单枚超时 300s，3 并发 worker；逐枚记录
tier/scene/风格轴/胞体/构图型/时长/JS 异常/像素统计/兜底标志/scene35 相邻板 L∞
（复用 run-tests.mjs「近似重复板保底」同款指纹点集与生产阈值）。零筛选零修改，纯观测。

## 体检总表

| 维度 | n | 废片率 | 兜底率 | 时长 p50 / p95 / max |
|------|---|--------|--------|----------------------|
| 2D (s30-32) | 640 | **0%** | — | 3.3s / 6.7s / 15.0s |
| 3D (s34) | 35 | **0%** | 28.6% (10/35) | 21.6s / 45.7s / 46.1s |
| 4D (s35) | 121 | **0%** | — | 23.1s / 57.7s / 59.0s |
| 升维 (s17) | 4 | **0%** | — | 13.8s / 17.1s / 17.1s |
| **合计** | **800** | **0 / 800 = 0.00%** | | |

废片六判据全部零命中：渲染超时 0 / JS 异常 0 / 黑屏 0 / 纯色平板 0 / 近空画布 0
（全库最低内容占比 0.113，画面成立）/ 拼板近重复板 0（s35 全 121 件相邻 L∞ 最小
3.14e-3 ≥ 3.1× 生产阈值 1e-3）。

自然轮盘落点 vs 权重表：2D 80.0%（表 78）/ 3D 4.4%（5）/ 4D 15.1%（16）/ 升维 0.5%（1），
采样噪声内吻合；2D 池内 s30/s31/s32 = 262/217/161 vs 38/38/24，s30 neon 4/262 ≈ 1.5%（表 1%），
s35 拼4:拼16 ≈ 66:34（表 70:30），texture grid:poisson = 151:9（锁 95:5）——概率表全线兑现。

## 最差 20 件一句话

零废片下「最差」= 10 枚 s34 兜底件 + 10 枚全库内容占比最低补位：兜底件多数仍是成立的大色块
抽象近景，最弱一枚（hover 兜底 0x638f6e91…）物件几乎缩没近纯底纹；补位集中「淡色 poisson
稀疏件」与「glome 拼16 小球重复观感」（L∞ 达标的已知审美残余）两类。

## 铸造就绪结论（草案，待 user 终裁）

1. **渲染层无铸造阻塞**：800 枚全维度零废片，时长包络 max 59s 远低于超时线。
2. 唯一系统性信号：s34 **closeup 型兜底 5/6（83%）**，显著高于其他四型（monument 11%/
   colossus 25%/hover 25%/stilllife 0%）——兜底是设计路径且观感多数成立，非阻塞，
   若在意自然放置成功率可下卷追根。
3. glome 拼 16 板间韵律观感近似为已知审美残余（判据不触发），与 cell120/600 去留同属
   内容策展层议题，不属渲染健康度。
